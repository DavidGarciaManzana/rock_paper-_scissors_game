import React from 'react';
import {PlayButton} from "./PlayButton";
function WaitButton() {
    return (
        <PlayButton  color={'gray'} url={'media/reloj.png'} description={'A blank button'} widthImage={65}></PlayButton>
    )
}

export {WaitButton}